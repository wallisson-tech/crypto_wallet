<p style="color: green"><%= notice %></p>

<%= render @mining_type %>

<%= link_to t("links.editing"), edit_mining_type_path(@mining_type), class: ('btn btn-primary') %> |
<%= link_to t("links.back"), mining_types_path, class: ('btn btn-primary') %>

<%= button_to "Destroy this mining type", @mining_type, method: :delete %>
